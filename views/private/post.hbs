<div class="container-fluid">
  <div class="posts">
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <hr />
    <h3>Comments</h3>
    {{#each post.comments as |comment|}}
    <p>{{comment.content}}<br />
    {{comment.user_id}} - {{comment.createdOn}}</p>
    {{/each}}

  </div>
</div>